// This file has been generated by beamsplitter

#include <image/LinearImage.h>
#include "../jsbindings_helpers.h"

#include <emscripten.h>
#include <emscripten/bind.h>

#include <string>
#include <vector>

using namespace emscripten;
using namespace filament;

EMSCRIPTEN_BINDINGS(filament_utils_LinearImage) {

class_<image::LinearImage>("image$LinearImage")
    .constructor<uint32_t, uint32_t, uint32_t>()
    .constructor<>()
    .function("getWidth", &image::LinearImage::getWidth)
    .function("getHeight", &image::LinearImage::getHeight)
    .function("getChannels", &image::LinearImage::getChannels)
    .function("reset", &image::LinearImage::reset)
    .function("isValid", &image::LinearImage::isValid)
    ;

// =============================================================================
// HANDWRITTEN SECTION
// =============================================================================
// The following could not be generated and need to be written by hand.
// Anything added below this comment block is preserved when regenerating.
//   - LinearImage::getPixelRef(): returns float *, an array with no length
//   - LinearImage::getPixelRef() const: returns const float *, an array with no length
//   - LinearImage::getPixelRef(uint32_t, uint32_t): returns float *, an array with no length
//   - LinearImage::getPixelRef(uint32_t, uint32_t) const: returns const float *, an array with no length


} // EMSCRIPTEN_BINDINGS
