// This file has been generated by beamsplitter

#include <ktxreader/Ktx2Reader.h>
#include "../jsbindings_helpers.h"

#include <emscripten.h>
#include <emscripten/bind.h>

#include <string>
#include <vector>

using namespace emscripten;
using namespace filament;

// Filament keeps the destructor of everything the engine owns to itself, so
// embind has to be told, once per class, that it cannot delete one.
namespace emscripten {
    namespace internal {
        template<> void raw_destructor<ktxreader::Ktx2Reader::Async>(ktxreader::Ktx2Reader::Async* ptr) {}
    }
}

EMSCRIPTEN_BINDINGS(filament_utils_Ktx2Reader) {

class_<ktxreader::Ktx2Reader>("ktxreader$Ktx2Reader")
    .constructor<filament::Engine&, bool>()
    .function("requestFormat", &ktxreader::Ktx2Reader::requestFormat)
    .function("unrequestFormat", &ktxreader::Ktx2Reader::unrequestFormat)
    ;

class_<ktxreader::Ktx2Reader::Async>("ktxreader$Ktx2Reader$Async")
    .function("doTranscoding", &ktxreader::Ktx2Reader::Async::doTranscoding)
    .function("uploadImages", &ktxreader::Ktx2Reader::Async::uploadImages)
    ;

// =============================================================================
// HANDWRITTEN SECTION
// =============================================================================
// The following could not be generated and need to be written by hand.
// Anything added below this comment block is preserved when regenerating.
//   - Ktx2Reader::load(const void *, size_t, TransferFunction): parameter "data": const void * is opaque, so there is nothing to marshal
//   - Ktx2Reader::asyncCreate(const void *, size_t, TransferFunction): parameter "data": const void * is opaque, so there is nothing to marshal
//   - Ktx2Reader::asyncDestroy(Async **): parameter "async" has unresolved type Async **
//   - Async::Async(const Async &): explicitly deleted
//   - Async::Async(Async &&): explicitly deleted
//   - Async::getTexture() const: return type: struct array with no element type


// The following C++ methods were not auto-generated and can be implemented here:

} // EMSCRIPTEN_BINDINGS
