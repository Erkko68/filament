// This file has been generated by beamsplitter

#include <utils/Entity.h>
#include "../jsbindings_helpers.h"

#include <emscripten.h>
#include <emscripten/bind.h>

#include <string>
#include <vector>

using namespace emscripten;
using namespace filament;

// Filament keeps the destructor of everything the engine owns to itself, so
// embind has to be told, once per class, that it cannot delete one.
namespace emscripten {
    namespace internal {
        template<> void raw_destructor<utils::Entity>(utils::Entity* ptr) {}
    }
}

EMSCRIPTEN_BINDINGS(filament_Entity) {

class_<utils::Entity>("Entity")
    .constructor<>()
    .function("equals", &utils::Entity::operator==)
    .function("notEquals", &utils::Entity::operator!=)
    .function("isNull", &utils::Entity::isNull)
    .function("getId", &utils::Entity::getId)
    .function("clear", &utils::Entity::clear)
    .class_function("smuggle", &utils::Entity::smuggle)
    .class_function("import", &utils::Entity::import)
    ;

// =============================================================================
// HANDWRITTEN SECTION
// =============================================================================
// Everything on this class was generated. Anything added below this comment
// block is preserved when regenerating.


class_<utils::Entity>("Entity")
    .function("getId", &utils::Entity::getId)
    ;

} // EMSCRIPTEN_BINDINGS
